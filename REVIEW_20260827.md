# 代码审查报告 — 2026-08-27

> 审查范围：后端全部模块 + 前端全部组件 + 测试/部署
> 方法：通读源码 + 运行测试（162 后端 + 119 前端，全部通过）+ 4 路子代理并行深审
> 已有 BUG_REPORT.md 不重复收录，仅报告**新的、可复现的、且不在 BUG_REPORT.md 里**的问题

---

## 一、审查总览

| 模块 | 文件数 | 问题数 |
|---|---|---|
| 后端-社交（friends/groups/pk/leaderboard/achievements） | 5 | 20 |
| 后端-核心（auth/catalog/daily/challenge/profile/misc/boss 等） | 10 | 13 |
| 前端（Vue 组件 + lib） | 20 | 15 |
| 测试/数据/脚本/部署 | 15+ | 15 |
| **合计** | **50+** | **63** |

严重度分布：**H 3 / M 24 / L 36**

---

## 二、严重问题（H — 建议立即修复）

### H1. pk.py：WebSocket 座位抢占竞态
- **位置**：`backend/pk.py:274-276`
- **描述**：`ws_pk` 中，非房主连接时检查 `row["opponent"] is None` 然后 UPDATE opponent。两个非房主**并发连接**时，两者都看到 opponent 为 None，都执行 UPDATE，后者的写入覆盖前者。
- **复现**：用户 A 创建房间 → 用户 B 和 C 同时通过 WebSocket 连接同一房间码 → C 的 seat 覆盖 B，B 变成 spectator
- **影响**：PK 对战可能变成 1v1 之外的局面；旁观者可能占座；对战结果不可信

### H2. pk.py：服务端不校验分数，客户端可刷分
- **位置**：`backend/pk.py:232-234, 136-139`
- **描述**：PK 对战中，服务端完全信任客户端上报的 `score` 值。`_upsert_result` 用 `MAX()` 合并，分数只升不降。客户端可以：
  1. 连接后直接发送 `{"type":"progress","score":30}` 声称满分
  2. 或发送 `{"type":"finish","score":30,"combo":30}` 直接"赢"
- **复现**：用 curl/ws 客户端发送伪造 progress 帧
- **影响**：PK 排行榜可被任何客户端刷分；README 声称的"服务端权威计时判分"在此环节不成立
- **备注**：每日挑战（daily.py:130-185）和冲刺挑战（challenge.py:208-240）都有服务端判分，唯独 PK 依赖客户端自报——这是三套对战机制中唯一不一致的

---

## 三、中等问题（M — 建议尽快修复）

### M1. friends.py：add/accept 并发导致重复动态
- **位置**：`backend/friends.py:213-218, 239-242`
- **描述**：`api_add` 中"对方先申请→自动互认"分支和 `api_accept` 都执行 `UPDATE friend_relation SET status='accepted'` 但**不带 `WHERE status='pending'`**。并发两个 accept 请求都通过检查后都执行 UPDATE 和 `record_activity`，产生**重复的 friend_join 动态**。
- **复现**：用户 A 和 B 互相申请后，B 快速连点两次"通过"
- **影响**：动态流出现重复事件；不影响关系状态但影响体验

### M2. friends.py：notify_level 并发导致重复升级动态
- **位置**：`backend/friends.py:322-334`
- **描述**：`notify_level` 先 SELECT 当前等级基线，再 UPDATE + record_activity。两个并发调用都读到旧基线，都 UPDATE，都 record_activity → 重复 level_up。
- **复现**：用户在两个标签页同时完成练习
- **影响**：重复升级通知

### M3. groups.py：join 并发突破人数上限
- **位置**：`backend/groups.py:254-259`
- **描述**：`api_join` 先 COUNT 检查 members < 20，再 INSERT。并发两个 join 都通过检查后都 INSERT，组内成员数变为 21。
- **复现**：20 人满员时两个用户同时点"加入"
- **影响**：人数上限可被突破；PK 房间码枚举风险类似（但上限是 1 而非 20，影响更小）

### M4. pk.py：Origin 校验端口不匹配
- **位置**：`backend/pk.py:253`
- **描述**：`urlparse(origin).netloc != request.host`。生产环境 nginx 代理到 gunicorn:8200，`request.host` 可能是 `mi2.cc.cd`（无端口），Origin 是 `https://mi2.cc.cd`（netloc=`mi2.cc.cd`）。但如果 nginx 配置了非标准端口透传，netloc 会带端口（`mi2.cc.cd:443`），与 request.host 不匹配。
- **影响**：特定 nginx 配置下 WebSocket 连接被拒
- **修复方向**：用 `urlparse(origin).hostname` 而非 `.netloc`，或忽略端口比较

### M5. pk.py：autofinish 后版本检查用旧值
- **位置**：`backend/pk.py:292-296`
- **描述**：`_maybe_autofinish(conn, room)` 在 DB 中递增 version，但紧随其后的版本检查 `if room["version"] > pushed_version` 用的是**autofinish 之前**的 `room`（line 286-288 获取的）。虽然 `fresh` 被重新获取（line 293-294）但只用于 state 检查，未用于 version 比较。
- **影响**：房间被自动收尾后，终局快照延迟一个 tick（0.5s）才推送
- **修复方向**：将 line 295 的 `room["version"]` 改为 `fresh["version"]`

### M6. challenge.py：sprint_challenge_score 竞态丢分
- **位置**：`backend/challenge.py:220-234`
- **描述**：与 pk_result 不同，`sprint_challenge_score` 的 UPSERT 用的是直接赋值（`score=excluded.score`）而非 `MAX()`。且无 `BEGIN IMMEDIATE`，read-check-write 非原子。并发两个提交中，较低分数可能覆盖较高分数。
- **复现**：同一用户快速提交两次不同分数
- **影响**：用户在挑战榜上的更好成绩可能丢失
- **对比**：pk_result 用 `MAX(pk_result.score, excluded.score)` 正确；sprint_best 用 `BEGIN IMMEDIATE` + tuple 比较正确；唯独此处三者兼备

### M7. auth_routes.py：时间戳格式不一致
- **位置**：`backend/auth_routes.py:144, 188`
- **描述**：`account.created_at` 和 `account.last_login_at` 使用 SQLite 的 `datetime('now')`，格式为 `"2026-08-27 10:00:00"`（空格分隔、UTC）。项目其他模块用 `now()`（catalog.py:51），格式为 `"2026-08-27T10:00:00"`（T 分隔、服务器本地时区）。
- **影响**：如果任何代码将 `account.created_at` 与其他表的 timestamp 比较，字符串比较会因格式差异产生错误结果
- **修复方向**：统一用 `now()` 或 `now_iso()`

### M8. auth_routes.py：并发失败重置限流计数
- **位置**：`backend/auth_routes.py:89-94`
- **描述**：`_record_failure` 中，当并发两个请求同时发现无行或窗口过期时，都尝试 INSERT。ON CONFLICT 的 UPDATE 将 `attempts=1` 重置。这导致并发暴力破解时，限流计数器被频繁重置到 1，实际效果弱于预期。
- **复现**：并发发送多个错误密码请求
- **影响**：限流在并发攻击下效果减弱

### M9. friends.py：api_accept 完全缺少 FRIENDS_MAX 校验
- **位置**：`backend/friends.py:221-243`（`api_accept`），对比 `:163-218`（`api_add`）
- **描述**：`api_add` 在 line 192-196 对 `me` 的 `accepted` 好友数做了 `if count >= FRIENDS_MAX` 校验，但 `api_accept` **完全没有对应校验**。当 B 向 A 发出好友申请（B 的好友数 < 100），而 A 的好友数已达 100 时，A 调用 accept 会直接 UPDATE，A 的好友数变为 101。
- **复现**：A 已有 100 好友 → B 向 A 发申请 → A 通过申请 → A 好友数 101
- **影响**：FRIENDS_MAX 设计为防关系表膨胀，accept 路径可绕过

### M10. auth_routes.py：注册邀请不校验 disabled_at 和 FRIENDS_MAX
- **位置**：`backend/auth_routes.py:149-162`
- **描述**：注册时若提供 `inviter` 参数（仅校验 `valid_user_id`），直接 INSERT 为 accepted，**既不校验邀请人是否已禁用（`disabled_at IS NULL`），也不校验邀请人的好友数是否已达上限**。
- **复现**：邀请人 I 被禁用 → 新用户注册提交 `inviter=<I的id>` → 关系行创建，I 好友数超限
- **影响**：已禁用账户仍可被"邀请"，关系行悬空；好友数可超限

### M11. groups.py：ACTIVE_CHALLENGES_MAX 并发可突破
- **位置**：`backend/groups.py:322-326`（`api_create_challenge`）
- **描述**：与 M3 同构。`SELECT COUNT(*) WHERE expires_at>=?` 与后续 INSERT 之间无原子性。两个并发创建挑战请求可同时读到 active=2，均通过校验，活跃挑战数=4。
- **复现**：组已有 2 个活跃挑战 → 两成员并发创建 → 活跃数=4

### M12. pk.py：Origin 头缺失时校验被完全跳过
- **位置**：`backend/pk.py:252-255`
- **描述**：`if origin and ...`——当 Origin 头缺失时（非浏览器 WS 客户端、wscat/websocat 等工具），整个校验被跳过。降低了跨站 WebSocket 劫持（CSWSH）防护。
- **复现**：用 wscat 不带 Origin 头连接 `/ws/pk/<code>`，直接通过校验
- **影响**：无 Origin 来源的 WS 连接可建立

### M13. friends.py 用 UTC / groups.py+pk.py 用本地时间
- **位置**：`backend/friends.py:14`（`now_iso` UTC）vs `backend/groups.py:17`、`backend/pk.py:30`（`now` 本地）
- **描述**：`friend_activity.created_at` 存 UTC 带时区（`"2026-09-15T08:30:00+00:00"`），`study_group`/`pk_room` 等存本地无时区（`"2026-09-15 16:30:00"`）。当前无跨模块比较故无功能 bug，但属代码质量隐患。
- **影响**：未来跨模块时间比较时产生静默错误

### M14. boss.py：Boss 战跨素材库误删 word_state
- **位置**：`backend/boss.py:106-107`
- **描述**：`UPDATE word_state SET wrong_count=0,status='new',next_review=NULL WHERE user=? AND item_id=?` **缺少 `list` 条件**。词库中大量词汇跨素材库重名（cet4/cet6/kaoyan 共享词汇，item_id 相同）。Boss 战清除一个库的错词时，其他库中同 item_id 的行也被一并重置。
- **复现**：用户在 cet4 和 cet6 都答错 `hello` → Boss 战打对 `hello` → cet4 和 cet6 的 `hello` 行均被重置为 wrong_count=0, next_review=NULL
- **影响**：**跨库数据污染**——其他素材库中同名词的错词状态被重置，next_review 置 NULL 导致该词从复习队列消失

### M15. catalog.py：api_result 缺少 right 校验，静默误判为答错
- **位置**：`backend/catalog.py:417-429, 473-484, 495-509`
- **描述**：客户端发送 `outcome=completed` 但不带 `right`/`first_right`/`final_right` 时：
  - `effective_first=None` → `1 if None else 0` → **first_right 写入 0（答错）**
  - `final_right=None` → **final_right 写入 0**
  - `update_word_state` 被跳过（记忆状态不变）
  - daily_log 记录 1 次错答，但 daily_practice_log 所有计数为 0
  - **四处口径不一致**
- **复现**：`POST /api/result {"session_id":"...","id":"hello","outcome":"completed"}` → 返回 ok:true 但 wrong_count+1，word_state 不变
- **影响**：恶意客户端可伪造"已完成但答错"记录污染准确率；前端漏传 right 字段时产生幽灵错答

---

## 四、低等问题（L — 可排期修复）

### L1. groups.py/challenge.py：UUID 截断碰撞风险
- **位置**：`backend/groups.py:65, 327`；`backend/challenge.py:179`
- **描述**：`uuid.uuid4().hex[:10]` 只保留 40 bit 随机性，无唯一性预检。碰撞概率极低（~2^20 条记录才有 50% 概率），但发生时 INSERT 会 IntegrityError → 500。
- **影响**：极低概率的 500 错误

### L2. daily.py/challenge.py/profile.py：date.today() 时区不一致
- **位置**：`backend/daily.py:108, 143`；`backend/challenge.py:54, 149`；`backend/profile.py:69`
- **描述**：`date.today()` 使用服务器本地时区。服务器在 UTC 时，UTC+8 用户在 00:00-08:00 之间看到的"今天"与服务器不同。
- **影响**：与 BUG_REPORT H12 同源但范围更广——影响每日挑战、冲刺、等级计算等多处

### L3. pk.py：WebSocket 循环未捕获的异常
- **位置**：`backend/pk.py:278-308`
- **描述**：`ws_pk` 的 while 循环只捕获了 `ConnectionClosed`。DB 错误、JSON 序列化错误、`_snapshot` 中的异常都会导致线程崩溃，连接无清理。
- **影响**：异常情况下 WS 线程泄漏

### L4. core.js：timeout=0 被当作无超时
- **位置**：`frontend/src/lib/core.js:49`
- **描述**：`opts.timeout || API_TIMEOUT`——0 是 falsy，会用默认 15s。与 BUG_REPORT L3 同源。
- **影响**：无法通过传 0 来禁用超时

### L5. pk.py：客户端发送 "join" 帧但服务端忽略
- **位置**：`frontend/src/components/PkPage.vue:135`；`backend/pk.py:222`
- **描述**：客户端 WS onopen 发送 `{"type":"join"}`，但服务端 `_handle_message` 不处理 "join" 类型（不在 `{"progress","finish","start"}` 中）。服务端在连接建立时即分配座位，join 帧多余。
- **影响**：无功能影响，但增加混淆

### L6. leaderboard.py：短名打码不一致
- **位置**：`backend/leaderboard.py:42-43`
- **描述**：`_mask_part` 对 len≤3 的名字只保留首字符（`s[:1] + "**"`），不保留尾字符。commit 信息声称"保留首尾轮廓"但实现只留首。
- **影响**：3 字符名字如"abc" → "a**"，尾字符被遮盖，与 >3 字符的行为不一致

### L7. friends.py：FRIENDS_MAX 并发突破
- **位置**：`backend/friends.py:192-196`
- **描述**：好友数上限检查无原子保护。并发两个 add 都通过检查后都 INSERT。
- **影响**：极小概率超过 100 好友上限

### L8. pk.py："start" 消息并发覆盖
- **位置**：`backend/pk.py:217-219`
- **描述**：两个并发 "start" 都 UPDATE started_at，后者覆盖前者。started_at 有微小偏差但功能不受影响。
- **影响**：计时起点有毫秒级偏差

### L9. pk.py：sealed 检查竞态
- **位置**：`backend/pk.py:226-229`
- **描述**：SELECT finished_at 和 UPDATE 之间，另一个 finish 帧可能先到达并完成写入。第二个 finish 的 sealed 检查已通过（因为第一个还没提交），但 MAX merge 会保留最高分。
- **影响**：极小概率的重复计分（但 MAX 保证只取最大值）

### L10. auth_routes.py：logout 硬编码 cookie 名
- **位置**：`backend/auth_routes.py:202`
- **描述**：`delete_cookie("dict_session")` 而非使用 `SESSION_COOKIE` 常量。当前值一致，但违反单一来源原则。
- **影响**：未来重命名 cookie 时易遗漏

### L11. groups.py/challenge.py：性能隐患
- **位置**：多处
- **描述**：
  - `_member_cards` 对每个成员调用 `derive_profile`（8 条 SQL）→ 20 人组 = 160 条 SQL/请求
  - `_challenge_payload` 对每个挑战分别查 3 张表 → 20 个挑战 = 60 条 SQL/请求
  - `_sweep_rooms` 全表扫描 `WHERE state='playing'`（无 state 索引）
  - `derive_profile` 被 friends/groups/leaderboard 多处复用但每次都全量重算
- **影响**：大数据量时响应变慢；当前规模无感知

### L12. auth.py：游客 CSRF token 不轮换
- **位置**：`backend/auth.py:231`
- **描述**：游客的 CSRF cookie 在 `resp()` 中被复用（`ident["csrf_token"] or request.cookies.get(...)`）。游客会话可能持续 10 年（cookie max_age=31536000）不换 token。
- **影响**：无安全影响（游客 token 不绑定服务端状态），但不符合 token 短期轮换的最佳实践

### L13. pk.py：sweep_rooms 频繁全表扫描
- **位置**：`backend/pk.py:111`
- **描述**：`_sweep_rooms` 在每次 `api_create_room` 时执行 `SELECT code, started_at FROM pk_room WHERE state='playing'`。无 `idx ON pk_room(state)` 索引，是全表扫描。
- **影响**：房间数多时创建房间变慢

### L14. groups.py：挑战窗口 off-by-one（8 天而非 7 天）
- **位置**：`backend/groups.py:43-44, 316`
- **描述**：`_today_iso()` 返回本地日期。`expires = date.today() + timedelta(days=7)` 得到 7 天后的日期。`_challenge_payload` 窗口为 `[start_day, end_day]` **闭区间**，含创建日和到期日，实际跨 **8 个自然日**。
- **复现**：9-15 创建 days=7 的挑战 → expires=9-22 → 9-22 当天仍 active → 实际活跃 8 天

### L15. achievements.py：Python 3 banker's rounding 边界误判
- **位置**：`backend/achievements.py:66-67`
- **描述**：`round(accuracy * 100)` 使用 banker's rounding。`round(89.5) = 90`（90 是偶数），准确率 89.5% 的用户会被判为 `90 >= 90` 解锁成就，但实际未达 90%。
- **复现**：200 次首答 179 次正确（89.5%）→ 成就解锁
- **修复方向**：用 `math.floor(accuracy * 100 + 0.5)` 或 `int(accuracy * 100 + 0.5)`

### L16. friends.py：已禁用好友在列表中可见但搜索不到
- **位置**：`backend/friends.py:115-116` vs `:88-91`
- **描述**：`api_search` 带 `AND disabled_at IS NULL`，但 `api_friends` 的 enrich 不过滤。已禁用好友在列表正常显示，但搜索不到。
- **影响**：行为不一致

### L17. leaderboard.py：不过滤已禁用账户
- **位置**：`backend/leaderboard.py` 全部聚合函数
- **描述**：排行榜从学习表查询，不关联 `account` 表、不过滤 `disabled_at`。已禁用账户的分数仍上榜。
- **影响**：可能是设计意图（历史分数不回滚），但与 api_search 的过滤不一致

### L18. challenge.py：sprint best 的 record 字段与 DB 更新口径不一致
- **位置**：`backend/challenge.py:153`（DB 更新用元组比较）vs `:165`（响应用 score 单独比较）
- **描述**：DB 更新条件 `(score, combo) > (old_score, old_combo)`（同分比连击），但响应字段 `score > old_score`（仅比分数）。用户刷出同分更高连击时 DB 更新但 `record:false`。
- **复现**：历史 best=100/50，新成绩=100/60 → DB 更新但响应 record:false
- **影响**：前端不触发新纪录动画，用户感知不到破连击纪录

### L19. memorize.py：memorize_count NULL 导致 TypeError
- **位置**：`backend/memorize.py:97-102`
- **描述**：`state["memorize_count"] += 1` 未做 NULL 保护。catalog.py 的同名函数已有 `if state.get(k) is None: state[k] = 0` 归一化，memorize.py 遗漏。
- **复现**：`UPDATE word_state SET memorize_count=NULL` → `POST /api/memorize` → 500 TypeError

### L20. catalog.py：session_context 的 isdigit() 对 Unicode 数字崩溃
- **位置**：`backend/catalog.py:74`
- **描述**：`lesson.isdigit()` 对 Unicode 上标数字（`²`）返回 True，但 `int("²")` 抛 ValueError → 500。同文件的 `clamp_int` 做了 try/except 但此处绕过。
- **复现**：`GET /api/session?list=nc1&lesson=²` → 500

### L21. auth_routes.py：认证限流 TOCTOU 竞态
- **位置**：`backend/auth_routes.py:69-80`
- **描述**：`_rate_limited`（SELECT）与 `_record_failure`（UPDATE）在 WAL 模式下读写不互斥。并发两个登录请求可同时读到 attempts=0，均放行。有效阈值退化为 `configured × 并发数`。
- **影响**：并发暴力破解时限流被绕过

### L22. catalog.py：follow 模式不计入 daily_log
- **位置**：`backend/catalog.py:498-509`
- **描述**：`if session["practice_mode"] != "follow":` 包裹 daily_log 的 INSERT。follow 模式写入 daily_practice_log 但不写 daily_log，两表覆盖范围不一致。依赖 daily_log 的接口（/api/lists 的 today、/api/stats 的 streak）在用户仅用 follow 模式时显示零活动。
- **影响**：follow 模式用户的打卡统计为零

---

## 五、前端问题

### H3. PkPage.vue：submitWord 错答也递增 myAnswered，可溢出 total
- **位置**：`frontend/src/components/PkPage.vue:228-231`
- **描述**：猜对和猜错都 `myAnswered.value++`。猜错后可反复重试同一词，每次递增并通过 `sendProgress()` 上报。`myAnswered` 可超过 `total`（词流长度），客户端 HUD 显示异常，服务端虽 clamp 到 9999 但语义错误。
- **复现**：PK 对战中连续错答同一词多次 → myAnswered 远超 total → 服务端 answered 字段虚高
- **影响**：PK 对战的 answered 统计失真；客户端 HUD 显示不一致

### M16. MemorizePage.vue：onDocDown 缺少鼠标非左键判断，右键菜单被拦截
- **位置**：`frontend/src/components/MemorizePage.vue:141-151`
- **描述**：对非 touch 指针一律 `preventDefault()`，未加 `if (ev.button !== 0) return` 守卫（PracticePage 的 H9 已修，此处遗漏）
- **复现**：背单词页右键点击 → 菜单不弹出

### M17. PracticePage.vue：onMounted 中 JSON.parse 无 try-catch，脏缓存致白屏
- **位置**：`frontend/src/components/PracticePage.vue:74-76`
- **描述**：`JSON.parse(sessionStorage.getItem("dict_custom"))` 无 try-catch。存储被污染或损坏时抛 SyntaxError，组件 onMounted 中断，页面白屏。
- **复现**：`sessionStorage.setItem("dict_custom", "{invalid")` → 刷新 → 白屏

### M18. PracticePage/MemorizePage：play() 内 ensureAudio 异常未捕获，未处理 Promise 拒绝
- **位置**：`frontend/src/components/PracticePage.vue:278-279`、`MemorizePage.vue:199-200`
- **描述**：`await ensureAudio(item)` 无 try-catch。网络错误时抛异常成为 unhandled rejection，触发浏览器控制台错误。play() 在多处被无 await 调用（next、onMounted、toggleScope）。
- **复现**：断网 → 点击播放 → 控制台 Uncaught (in promise)

### M19. PkPage.vue：断线重连后 localDone 已锁但 finish 消息丢失，用户卡死
- **位置**：`frontend/src/components/PkPage.vue:264-271, 188-196`
- **描述**：倒计时归零时 `finishLocal()` 发送 finish 并置 `localDone=true`。若此时 WS 断线，finish 丢失。重连后服务端下发 `phase:"playing"`，但 `localDone` 已锁，`finishLocal` 的 `if (localDone) return` 阻止重发。用户无法输入、无法提交、倒计时重新归零也不再发 finish。
- **复现**：PK 对战倒计时接近 0 时断网 → 重连成功 → 用户卡死必须刷新

### L23. SentenceCells.vue：纯标点 token 的字符集不含标点，该词格永远无法填满
- **位置**：`frontend/src/components/SentenceCells.vue:16-24, 44`
- **描述**：纯标点 token（如 "—"、"!"）的 core 被设为全量，但 `typeWordChar` 白名单 `/^[a-zA-Z0-9_'\-.,/&:]+$/` 不含 "—"、"!"、"?"。用户无法输入这些字符，isCorrect 永远 false。
- **复现**：含独立标点（破折号、感叹号）的句子 → 对应词格无法输入

### L24. PracticePage.vue：restoreInputSnapshot 脏 JSON 不清除，持续解析失败
- **位置**：`frontend/src/components/PracticePage.vue:484-488`
- **描述**：catch 块仅 `/* ignore */`，未清除损坏的 sessionStorage 键。后续每次进入同题都重复失败，输入恢复永久失效。
- **复现**：`sessionStorage.setItem("dict_input:xxx:yyy", "{bad")` → 输入框为空且刷新无效

### L25. GroupDetailPage.vue：leave/dissolve 成功后 actionLoading 未复位
- **位置**：`frontend/src/components/GroupDetailPage.vue:61-71, 73-83`
- **描述**：成功后改 hash 导致组件卸载，actionLoading 不复位。若路由切换失败，按钮永久停在"退出中…"状态。
- **复现**：退出小组 → 若路由切换失败 → 按钮不可操作

### L26. FriendsPage.vue：timeAgo 对非法 ISO 时间串返回 "NaN 天前"
- **位置**：`frontend/src/components/FriendsPage.vue:162-174`
- **描述**：后端返回 null/空串/格式错误时间时，`new Date` 返回 Invalid Date，`getTime()` 返回 NaN，最终显示 "NaN 天前"。
- **复现**：好友 last_active_at 为 null → 显示 "NaN 天前"

### L27. GroupDetailPage.vue：challengeTitle 非法日期静默回退 7 天
- **位置**：`frontend/src/components/GroupDetailPage.vue:122-136`
- **描述**：`created_at`/`expires_at` 非法时 `diff=NaN`，条件为 false，days 保持默认 7。即使实际窗口 14 天也显示"7 天"。
- **复现**：挑战 created_at 为 null → 标题显示"7 天"而非实际天数

### L28. WordCells.vue：restore 不重置 lastHit，恢复后残留击键高亮
- **位置**：`frontend/src/components/WordCells.vue:69-75`
- **描述**：`restore` 重置了 input/wcur/mark/extraInput 但未重置 lastHit。恢复后刚打的字母仍带 "hit" 高亮动画。
- **复现**：输入部分字母 → 刷新 → 恢复后最后一个字母高亮残留

## 六、测试/数据/脚本/部署问题

### M20. .gitignore 排除 BUG_REPORT.md 和 MIGRATION_PLAN.md
- **位置**：`.gitignore:19`（`*.md` 仅豁免 README.md）
- **描述**：`git check-ignore -v BUG_REPORT.md` → 被忽略。审查报告和迁移计划等审计文档不在版本控制中，审查历史无法追溯。
- **验证**：`git ls-files | grep -E "BUG_REPORT|MIGRATION"` → 空输出

### M21. .gitignore 排除 3 个关键音频脚本
- **位置**：`.gitignore:23-25`
- **描述**：`ingest_nce_audio.py`、`split_nce_audio.py`、`fix_nce_last_sentences.py` 被排除。全新克隆后无法生成 audio/ 目录，全程 fallback 到 edge-tts lazy 模式。
- **验证**：`git ls-files scripts/` → 只有 5 个文件，缺少 3 个

### M22. build_data.py / build_nce.py 非原子写入
- **位置**：`scripts/build_data.py:143`、`scripts/build_nce.py:76`
- **描述**：`Path.write_text()` 直接写入目标路径，无 temp+replace。写入中断后留下损坏 JSON，且下次运行因 `if out.exists(): skip` 跳过损坏文件，问题固化。
- **对比**：`split_nce_audio.py:49-78` 正确使用了 `tmp + os.replace` 原子模式

### M23. FriendsPage.test.js "reloads friends after addFriend" 是空转测试
- **位置**：`frontend/src/components/FriendsPage.test.js:74-84`
- **描述**：测试手动调用 `mockApi` 然后断言调用次数增加，不通过组件触发任何行为。改为 `expect(1).toBe(1)` 仍通过——验证了 mockApi 可调用而非组件逻辑。

### M24. check_runtime.py 不检查数据库 schema 和可访问性
- **位置**：`scripts/check_runtime.py` 全文
- **描述**：仅检查静态产物和素材加载。不检查：DB 文件是否存在/可读、schema 是否当前、SQLite 写权限、音频目录可写性。check 通过不代表服务能跑。

### L29. test_pk.py:115 `or True` 死代码断言
- **位置**：`tests/test_pk.py:115`
- **描述**：`assert second.json["role"] == "creator" or True`——恒为 True，永远通过。role 是 "spectator" 也通过。应删除或改为实际断言。

### L30. systemd 服务 --threads 4 被静默忽略
- **位置**：`systemd/english-dictation.service:10`
- **描述**：`--workers 2 --threads 4` 但未指定 `--worker-class gthread`。gunicorn 默认 sync worker，`--threads` 仅在 gthread/gevent 下生效。实际效果是 2 个 sync 进程各处理 1 请求，并发槽位从预期的 8 降到 2。
- **影响**：SQLite WAL 下 2 个并发写请求可能触发 busy_timeout=5000ms 等待

### L31. .claude/ 未加入 .gitignore
- **位置**：`.gitignore` 全文
- **描述**：`.claude/settings.local.json`（含本地权限配置）不在 .gitignore 也不在 git ls-files。`git add .` 会意外提交。

### L32. pytest-timeout 不在 requirements.txt
- **位置**：`requirements.txt`（4 行，无 pytest-timeout）
- **描述**：验证命令 `--timeout=60` 需要 pytest-timeout 插件但未安装 → `ERROR: unrecognized arguments`。

### L33. NOTICE 未覆盖 edge-tts 音频版权
- **位置**：`NOTICE` 全文
- **描述**：列出了三个素材来源但没提 edge-tts（Microsoft TTS）生成音频的版权限制。Azure Cognitive Services 对生成内容的商用再分发有明确限制。

### L34. gen_audio.py asyncio.gather 无 return_exceptions=True
- **位置**：`scripts/gen_audio.py:83`
- **描述**：任一 worker 异常即杀死全部 gather，已完成结果丢失。脚本退出码为 1 但无法判断哪些已完成。

### L35. fix_nce_last_sentences.py 不检查 nce_audio_map.json 存在
- **位置**：`scripts/fix_nce_last_sentences.py:109-110`
- **描述**：直接 `json.loads(map_path.read_text())` 无 exists 检查。对比 `split_nce_audio.py:93` 正确做了 `sys.exit(f"缺少...")`。

### L36. CI 缺 timeout-minutes / Python 矩阵 / 安全扫描 / concurrency
- **位置**：`.github/workflows/verify.yml` 全文（31 行）
- **描述**：无超时限制、仅测试 3.11（本地 venv 是 3.9）、无 pip-audit/npm audit、无 concurrency group、pull_request 无 types 限制。

| 类别 | 数量 | 状态 |
|---|---|---|
| 后端 pytest | 162 | ✅ 全部通过 |
| 前端 vitest | 119 | ✅ 全部通过 |

**测试缺口**：所有测试均为顺序执行，**无任何并发/竞态测试**。上述 H1/H2/M1/M2/M3/M6 等竞态问题均无法被现有测试发现。建议：
1. 用 `threading` 或 `concurrent.futures` 模拟并发请求
2. 用 `BEGIN IMMEDIATE` + 事务隔离级别测试
3. PK WebSocket 可用 `flask-sock` 的 test client 模拟多连接

---

## 七、修复优先级建议

| 优先级 | 项目 | 工作量 |
|---|---|---|
| P0 | H2 PK 客户端刷分（影响公平性） | 中 — 需服务端校验或改判分模型 |
| P0 | H1 PK 座位抢占（加 `WHERE opponent IS NULL`） | 小 |
| P0 | M12 Origin 头缺失绕过（CSWSH 防护） | 小 |
| P1 | M6 sprint_challenge_score 竞态（改 MAX + BEGIN IMMEDIATE） | 小 |
| P1 | M3/M11 groups 上限突破（改 INSERT 带条件或触发器） | 小 |
| P1 | M14 Boss 跨库误删 word_state（加 `AND list=?`） | 小 |
| P1 | M15 api_result 缺 right 校验（加显式校验或默认值） | 小 |
| P1 | M9 api_accept 漏 FRIENDS_MAX 校验 | 小 |
| P1 | M10 注册邀请不校验 disabled_at | 小 |
| P1 | M7 时间戳格式统一 | 小 |
| P1 | M20/M21 .gitignore 排除审计文档和音频脚本 | 小 |
| P2 | M1/M2 重复动态（加 `WHERE status='pending'` 或唯一约束） | 小 |
| P2 | M4/M5 PK WebSocket 小修 | 小 |
| P2 | M13 UTC/本地时间不一致 | 小 |
| P2 | M22 build_data 非原子写入（改 tmp+replace） | 小 |
| P2 | M23 空转测试（重写或删除） | 小 |
| P3 | L30 systemd --threads 被忽略（加 --worker-class gthread） | 小 |
| P3 | L29 死代码断言（删除 or True） | 小 |
| L31-L36 | CI/脚本/版权等 | 按需 |

---

## 八、整体评价

**优点**：
- 代码注释详尽，设计意图清晰（尤其 pk.py 的 docstring 和 scheduler.py 的 FSRS 公式注释）
- 测试覆盖充分（281 个测试），社交功能测试尤为完整
- SQLite 事务使用规范（BEGIN IMMEDIATE 用于关键竞态点）
- 判分哲学一致（daily/challenge 服务端判分，不信任客户端）
- 数据模型简洁，无过度工程化
- 依赖极少（Flask + SQLite + edge-tts），部署简单

**主要风险**：
- PK 对战的客户端信任模型与项目其他对战机制不一致（H2）——PK 是唯一不服务端判分的对战模式
- 多处并发竞态未加保护（H1/M3/M9/M11/M14/M15/M6）——多 worker gunicorn 部署下这些竞态是**真实可复现**的
- Boss 战跨库误删（M14）——数据污染，影响复习队列
- api_result 口径不一致（M15）——客户端漏传字段时四处数据不一致
- 审计文档和音频脚本被 gitignore 排除（M20/M21）——项目可追溯性和可重建性受损
- systemd --threads 被静默忽略（L30）——实际并发能力仅为预期的一半
- 时间戳格式/时区不统一（M7/M13）——长期维护隐患
- 无任何并发测试（M1 测试缺口）——竞态问题无法被 CI 发现
- derive_profile 性能瓶颈（L11）——用户量增长后会暴露
