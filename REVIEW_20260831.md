# 代码审查报告 — 2026-08-31

> 审查范围：P0/P1/P2 修复提交（c172591/bcbb779/113343f）逐项实测验证 + 后端/前端新问题审计
> 方法：三路并行深审，所有关键发现均用一次性并发/重放探针**实测复现**（探针用完即删）；基线 183 后端测试全绿、88 前端测试全绿
> 基线：REVIEW_20260827.md（63 项）、BUG_REPORT.md（30 项）——本报告只收录**修复验证结论**与**新发现问题**

---

## 一、上轮修复验证结论（实测）

| 结论 | 项 |
|---|---|
| ✅ 已修复且正确（16 项） | H1 座位抢占（条件 UPDATE）、M12/M4 Origin 校验、M14 Boss 跨库误删、M9/M10 好友上限/邀请校验、M7/M13 时间戳统一、M1/M2 重复动态（rowcount 守卫）、M5 autofinish 版本、M20/M21 gitignore、M22 原子写入、M23 空转测试重写 |
| ❌ **未修复（P1 提交中被跳过）** | **M6** 冲刺分数竞态丢分、**M3** 小组人数上限突破、**M11** 挑战数上限突破——`challenge.py`/`groups.py` 在三个修复提交中零改动 |
| ⚠️ **修复引入回归** | **M15**：`catalog.py:425` `first_right = not data.get("retried")` 删掉了 `raw_right and` 项，`right=false` 会写出 `first_right=1, final_right=0` 自相矛盾行（实测复现），污染正确率统计 |
| ⚠️ 已修复但未达目的 | **H2**：判分已迁服务端（`_record_answer` 逐题比对），但答案原文仍随快照下发 + 无计时闸门——见下方新发现 H1 |

### 未修复项实测证据

- **M6**（`challenge.py:220-234`）：仍是 `with db()`（非 IMMEDIATE）+ `DO UPDATE SET score=excluded.score` 直接赋值。探针：先写 90 → 后写 80 → 最终留 80，高分被低分覆盖。
- **M3**（`groups.py:248-262`）：COUNT-then-INSERT 无原子性。探针：上限调 2，4 线程并发 join → 组内 7 人。
- **M11**（`groups.py:320-336`）：同构。探针：上限调 1，12 线程并发创建 → 12 条活跃挑战。

---

## 二、新发现问题（后端）

### H1. PK 服务端判分被快照泄露的答案原文绕过
- **位置**：`backend/pk.py:235`（`"items": json.loads(row["items"])` 全量下发）+ `pk.py:186`（判分依据的 `expected` 即在快照中）
- **实测**：连房 → start → 读快照 items → 循环发 `{"type":"answer","index":i,"text":items[i].text}` → finish。30 题全对 <1s，`pk_result.score=30`。
- **影响**：P0 修复的方向对（服务端逐题比对），但判分依据明文发给所有连接（含对手），刷分从"假 score"变成"合法答案"，更隐蔽。PK 战绩/排行/成就仍不可信。
- **修法**：快照只下发 `id/phonetic/meaning/audio`，`text` 不下发（判分时服务端用 id 反查）；另加最小计时闸门（finish 时校验 `finished_at - started_at >= 题数 × 下限秒数`）。

### H2. `/api/result` legacy 路径（不带 session_id）可无限重放刷经验
- **位置**：入口 `catalog.py:448-449`，实现 `catalog.py:550-564`
- **实测**：`POST /api/result {"id":"hello","list":"test_words","right":true}` 连发 5 次 → `final_right_count=5`（+50 XP），且 `word_state.right_count=5`、`status='known'`、`next_review` 被推远。
- **影响**：无去重/无上限/无限流。`final_right`（权重 10）是 XP 公式最大项；XP 驱动等级、排行榜、好友动态、成就。同时污染 FSRS 复习队列——刷分 + 数据造假双重危害。游客即可执行。

### M1-M4. 计分接口重放/自报家族（与 H2 同根因）
| 接口 | 问题 | 实测 |
|---|---|---|
| `/api/match/result`（`match.py:80-95`） | `right` 纯客户端自报，无去重 | 4 对连发 3 次 → +120 XP |
| `/api/boss/result`（`boss.py:87-108`） | `right` 自报；答错不减 wrong_count，错词永不离开错词本，可无限重交 | 3 次答错 → first_wrong_count=3 |
| `/api/memorize`（`memorize.py:81-95`） | attempt_id 完全可选；不带可无限重放 | 连发 3 次 → memorize_count=3 → 误标"已背" |
| `/api/arrange/answer`（`arrange.py:119-131`） | 服务端真判分但无去重 | 正确排列连发 3 次 → +30 XP |

**统一修法（一处改动收敛 H2+M1-M4）**：所有计分写接口加强制 `attempt_id`（幂等键，memorize 的 memorize_attempt 表已是样板）+ 单接口单日次数上限 + 适度限流。

### M5. `/api/groups/<gid>` 只查登录不查成员，绕过同级接口的 403
- **位置**：`groups.py:186-215`（`api_detail` 仅 `_require_auth()`；对照 `:226` challenges 接口有成员检查）
- **实测**：非成员 GET `/api/groups/<gid>` → 200，返回完整 challenges（含 scores）、全部成员卡片（user_id/等级/连续天数）、创建者全名，响应里同时写着 `is_member: false`。
- **修法**：`api_detail` 对非成员只返回公开字段（组名/人数/是否可加入），成员数据走成员检查。

### M6. 五个接口未校验类型 → 游客单请求稳定 500
- **位置**：`boss.py:86`、`match.py:80`（unhashable dict id → TypeError）；`custom.py:71`、`groups.py:56`（`.strip()` on list → AttributeError）；`catalog.py:536`（mode 为 list → sqlite3.InterfaceError）
- **修法**：入参先 `isinstance` 校验再使用（`arrange.py` 的严格校验是样板）。

### L1-L4
- **L1** `legacy_result` 的 `mode` 无枚举校验：`"mode":123` 落库 → `/api/stats` 出现伪分类 `123`（`catalog.py:560-563` + `misc.py:143`）
- **L2** `sprint_best.updated_at` 写日期串而非时间戳（`challenge.py:149`），同日多条排序不定
- **L3** PK 单人房间终局 `winner: null`（`pk.py:227-231` 仅 2 人算赢家，但 `:306` 允许单人收尾）
- **L4** 排行榜 top-100 明文暴露完整 user_id（`leaderboard.py:163`）——无直接冒充路径（`_account_exists` 已挡），但 uuid 可枚举

---

## 三、新发现问题（前端）— 整体健康，无高危

XSS/Blob 泄漏风险面为零（全 src 无 v-html/innerHTML/eval/createObjectURL），鉴权/CSRF/服务端判分三处信任边界守住。剩余问题同根因：**异步窗口内缺"这还是原来那次操作吗"的守卫**。

### M1. `PracticePage.vue:407-428` toggleScope 不清 nextTimer
答对后 1.1s 内切「全部/已背」→ stale `next()` 打到新 session：已背集合 ≤1 题时用户被直接踢回目录页，否则静默跳题。**一行修**：`toggleScope()` 开头 `clearTimeout(nextTimer.value)`。

### M2. `MemorizePage.vue:339-358` quizNext 无 saving 守卫
quiz 阶段回车后 POST 在途时再按回车 → 在途 POST 落回新卡片，队列污染（答 1 张前进 2 次）。移动端弱网下是自然误触。**一行修**：`quizNext()` 顶部 `if (saving.value) return;`。

### M3. `ShareCard.vue:75` 伪对话框
`role="dialog"` 但无 `aria-modal`、无 Escape、无焦点陷阱/归还——键盘/读屏用户可操作遮罩下的页面。

### L1-L4
- **L1** `SettingsPage.vue:44-58` 三个数值设置零钳制且消费端不一致（replayTimes 填 999 真会重播 998 次；清空 → NaN 静默失效）
- **L2** `GroupsPage.vue:100-117` 搜索乱序响应（旧查询后发先至覆盖新结果；FriendsPage 同模式）
- **L3** `ShareCard.vue:36-44` watcher 以 payload 对象身份为键，父组件重渲染即整幅 canvas 重绘
- **L4** `WrongPage.vue:53-56` remove() 失败无 catch，断网点删除无任何反馈

### 测试质量附注
88 个测试无空转，全部真实驱动组件断言真实输出。但 `cards.test.js` 27 个用例中 **12 个覆盖的是生产无调用者的死代码**（`kind="sprint"` 分享卡从未被使用，SprintPage 自建内联分享）——删掉比保留划算。跨时序竞态（M1/M2 这类）完全无测试触达，建议补两个 `vi.useFakeTimers()` 卡住 POST 的回归用例。

---

## 四、纠正上轮误报

**REVIEW_20260827 L30（systemd --threads 被静默忽略）是误报**：gunicorn 23 在 `threads>1` 时自动将 worker_class 切换为 gthread（`gunicorn/config.py:109-120` 实测验证）。生产 `--workers 2 --threads 16` 实际生效，32 个并发槽位。无需处理。

---

## 五、修复优先级

| 优先级 | 项 | 工作量 |
|---|---|---|
| P0 | H2+M1-M4 计分接口统一加 attempt_id 幂等 + 单日上限（一处模式改动收敛 5 个接口） | 中 |
| P0 | H1 PK 快照不下发 text + 最小计时闸门 | 中（需前端配合） |
| P0 | M15 回归：恢复 `raw_right and` 语义（`catalog.py:425`） | 小 |
| P1 | M6/M3/M11 补修（BEGIN IMMEDIATE + 条件 INSERT/UPDATE，friends.py 已有样板） | 小 |
| P1 | M5 小组详情非成员字段裁剪 | 小 |
| P1 | 前端 M1/M2 两行守卫 | 小 |
| P2 | M6 五接口类型校验、前端 M3 对话框、L1 mode 枚举 | 小 |
| P2 | 补并发回归测试（H1/M1/M2 竞态各一探针入库）+ 删 cards.test.js 12 个死代码用例 | 小 |
| P3 | L1-L4 余项 | 按需 |

---

## 六、整体评价

三轮修复的质量本身是高的（条件 UPDATE、rowcount 守卫、服务端判分模式都正确），但有两个系统性缺口：

1. **修复覆盖面有洞**：P1 批跳过了 M6/M3/M11 三个并发项，M15 修复引入回归——修复提交缺一轮"提交后实测验证"环节。
2. **经验值写入面无防重放设计**：五个计分接口 + PK 快照泄露构成一条游客可执行的完整刷分流水线（H2→M1-M4→H1）。XP 是等级/排行/成就/好友动态的总水源，这是当前对公平性影响最大的问题簇，应作为下一个修复批的核心。

前端架构健康度明显好于后端写入面：问题集中在局部异步守卫遗漏，且正确样板（SprintPage 的 deadline 锚定 + locked 硬锁）就在同仓，属于未抄齐而非不会。
